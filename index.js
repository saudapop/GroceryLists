/**
 * @format
 */

import { AppRegistry } from "react-native";
import App from "Listables/App";
import { name as appName } from "Listables/app.json";

AppRegistry.registerComponent(appName, () => App);
