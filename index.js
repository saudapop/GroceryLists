/**
 * @format
 */

import { AppRegistry } from "react-native";
import App from "GroceryLists/App";
import { name as appName } from "GroceryLists/app.json";

AppRegistry.registerComponent(appName, () => App);
